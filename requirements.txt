# RockGuard · Streamlit Cloud 部署依赖
# 精简版 — 仅保留 Streamlit 运行必需的包

# --- YOLO 模型推理 (自动拉取 torch + numpy) ---
ultralytics>=8.3.0

# --- 计算机视觉 (headless = 无 GUI 依赖, 适合服务器) ---
opencv-python-headless>=4.10.0

# --- 配置管理 ---
python-dotenv>=1.0.0

# --- HTTP 客户端 (PushPlus 推送) ---
requests>=2.28.0

# --- 数据库 ---
sqlalchemy>=2.0.0
pymysql>=1.1.0

# --- Streamlit ---
streamlit>=1.40.0
pandas>=2.0.0

# --- 性能监控 ---
psutil>=5.9.0

# --- 科学计算 (SORT 匈牙利算法) ---
scipy>=1.10.0

# --- Excel 导出 ---
openpyxl>=3.1.0
