# RockGuard · Streamlit Cloud 部署依赖
# 包含完整 ML 推理链: YOLO + OpenCV + PyTorch CPU
# Streamlit Cloud 自动读取此文件: pip install -r requirements.txt

# --- YOLO + FastSAM 模型推理 ---
ultralytics>=8.3.0

# --- 计算机视觉 (headless 版适配无 GPU 服务器) ---
opencv-python-headless>=4.10.0

# --- 数值计算 ---
numpy>=1.26.0,<2.0.0

# --- PyTorch CPU (Streamlit Cloud 无 GPU) ---
torch>=2.0.0
torchvision>=0.15.0

# --- 配置管理 ---
python-dotenv>=1.0.0

# --- HTTP 客户端 (PushPlus 推送) ---
requests>=2.28.0

# --- 数据库连接池 ---
sqlalchemy>=2.0.0
pymysql>=1.1.0
alembic>=1.13.0

# --- Web 服务 (FastAPI) ---
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.9
jinja2>=3.1.0
starlette>=0.40.0

# --- Streamlit Web 封装 ---
streamlit>=1.40.0
pandas>=2.0.0
watchdog>=6.0.0

# --- 性能监控 ---
psutil>=5.9.0

# --- 科学计算 (SORT 匈牙利算法) ---
scipy>=1.10.0

# --- Excel 导出 ---
openpyxl>=3.1.0

# --- 加密 + 安全 ---
cryptography>=42.0.0

# --- Prometheus 监控 ---
prometheus-client>=0.21.0

# --- Sentry 错误监控 ---
sentry-sdk[fastapi]>=2.0.0
