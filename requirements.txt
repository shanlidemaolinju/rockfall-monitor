# ============================================================
# 落石检测系统 — Python 依赖
# ============================================================
# 安装: pip install -r requirements.txt
# Streamlit Cloud 一键部署: 自动识别此文件
# ============================================================

# --- 核心依赖 (rockfall/) ---
ultralytics>=8.3.0         # YOLO + FastSAM (内置CLIP, 无需额外安装)
opencv-python-headless>=4.10.0   # headless 版适配云部署 (无GUI依赖)
numpy>=1.26.0
requests>=2.28.0
python-dotenv>=1.0.0
pymysql>=1.1.0

# --- 桌面应用 (desktop/), 本地开发用 ---
PyQt6>=6.7.0

# --- Web 服务 (server/) ---
fastapi>=0.115.0
uvicorn>=0.34.0
python-multipart>=0.0.9
jinja2>=3.1.0

# --- Streamlit Web 封装 (app.py) ---
streamlit>=1.40.0
pandas>=2.0.0
torch>=2.0.0     # PyTorch (ultralytics 依赖, CPU 版即可)
